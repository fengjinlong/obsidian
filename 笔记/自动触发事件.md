# 元素自动触发事件
```js

<template>
  <button ref="btn" @click="cb">click</button>
</template>
<script setup>
import { ref, onMounted } from "vue";
const btn = ref(null);

let event = new Event("click");
onMounted(() => {
  btn.value.dispatchEvent(event);
});

const cb = () => {};
```