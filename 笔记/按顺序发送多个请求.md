```js
function fetch1(url) {
  return new Promise((resolve, reject) => {
    setTimeout(() => {
      resolve(url + "--- data");
    }, 1500);
  });
}
async function* fetchDate([...urls]) {
  for (let url of urls) {
    const res = await fetch1(url);
    yield res;
  }
}

async function getDate(urls) {
  // const g = fetchDate(urls);
  // let result;
  // while (!(result = await g.next()).done) {
  //   console.log(result.value);
  // }

  // 结构简写
  for await (let res of fetchDate(urls)) {
    console.log(res);
  }
}
getDate(["url1", "url2", "url3"]);
```